import { TimeTable } from 'components/TimeTable'

export const DemoTimeTable = () => {
  return (
    <main
      style={{
        width: '100%',
        minWidth: '1024px',
        margin: '2rem auto',
        padding: '2rem',
      }}
    >
      <TimeTable />
    </main>
  )
}
